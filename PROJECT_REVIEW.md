# signglass 프로젝트 검토 및 12주 실행안

검토 기준: 2인 팀, 12주, 캡스톤 디자인 경진대회 입상 목표.

## 결론

“완전한 독립형 양방향 스마트글래스”라는 최종 목표는 유지한다. 다만 모든 기능을 같은
완성도로 약속하면 2명·12주 일정이 무너지므로, 하나의 코드에서 세 단계로 완성도를 쌓는다.

| 단계 | 12주 목표 | 실패 시에도 남는 결과 |
|---|---|---|
| 필수 완성선 | 1:1 실내, 8~10개 의도 수어→음성, 한국어 음성→고정 HUD 자막 | 양방향 라이브 데모 |
| 경쟁 목표 | 15개 어휘, 하향 손+전방 얼굴 두 카메라, 얼굴 앵커 말풍선 | 고정 자막으로 즉시 전환 |
| 연구 확장 | 더 긴 연속 수어, 화자 분리, 거리별 3D 보정 | 별도 실험 결과로 발표 |

따라서 얼굴 추적 AR을 지금 배제할 필요는 없다. 현재 코드는 한 카메라, 두 카메라
(`--face-camera`), 카메라 없는 고정 자막(`--fixed-caption`)을 모두 지원한다. 실측 결과에 따라
시연 기본값만 바꾸면 된다. 연산부는 초기에는 노트북 또는 허리 장착 Pi에 두고, 기능 검증 후
경량화한다.

입상 가능성은 기능을 더 넣는 것보다 실제 사용자 문제, 물리적으로 완성된 형태, 정직한
실험 설계, 라이브 데모 안정성에서 올라간다.

## 가장 큰 한계와 의사결정 게이트

### 1. 한 카메라로 두 방향을 보는 기구적 모순 — 매우 높음

Phase 1은 착용자의 가슴 앞 손을 봐야 하고 Phase 2의 말풍선은 전방 상대 얼굴을 봐야 한다.
전방 카메라 한 대는 두 영역을 동시에 안정적으로 담기 어렵다. 더구나 MediaPipe Holistic은
제3자 정면 전신에 유리하고, 안경 일인칭 시점에서는 자기 어깨·손이 잘리거나 크게 왜곡된다.

2주차 종료 조건:

- 목표 안경 위치에 카메라를 고정하고 실제 수어 10개를 촬영한다.
- 프레임의 90% 이상에서 필요한 손이 검출되고, 상대 얼굴도 대화 거리에서 검출되는지 잰다.
- `python tools/check_mount.py --duration 30 --preview` 결과 JSON을 설계 증거로 보관한다.
- 실패하면 두 카메라(하향 손/전방 얼굴), 가슴 카메라, 또는 Phase 1 외부 카메라 중 하나를
  즉시 선택한다. 이 결정을 미루고 모델부터 학습하면 데이터를 다시 찍게 된다.

`landmarks.extract()`는 `wearable-v3`에서 손을 손목/손바닥 기준으로 분리 정규화하도록 수정했다.
따라서 어깨가 잘려도 손 특징은 유지된다. 다만 실제 장착 영상에서 손 자체가 MediaPipe에 잡히는지는
소프트웨어만으로 보장할 수 없다. 선택지는 다음 순서가 좋다.

1. 하향 안경 카메라에서 손 검출률 90% 이상이면 그대로 수어 인식에 사용한다.
2. 부족하면 가슴 카메라로 낮추되, 착용형 장치라는 전체 콘셉트는 유지한다.
3. 얼굴 앵커에는 전방 저해상도 얼굴 카메라를 별도로 두되, 두 카메라 각각의 실측 FPS와
   발열 기준을 통과한 경우 경쟁 목표로 유지한다.

### 2. 현재 수어 인식은 연속 수어 번역이 아님 — 매우 높음

현재 구조는 고정 길이 창의 고립 단어 분류기와 규칙 기반 문장 조합기다. 자연스러운 연속
한국수어의 시작·끝 검출, 동시적 비수지 신호, 자유 문장 번역은 해결하지 않는다. 발표에서는
“8~10개 의도/표현을 인식한다”고 정확히 표현하고 범용 수어 번역이라고 부르지 않는다.

### 3. 데이터 일반화 — 매우 높음

현재 직접 수집 데이터와 배포 모델이 없다. 이전 검토에서 AI Hub를 정면 2D 자료로만 본 것은
잘못이었다. 보유 샘플에는 3D 손/몸/얼굴과 카메라 보정값이 있다. WORD ID로 손 모양을
사전학습할 수 있고, 착용 데이터로 미세조정하는 경로를 추진할 수 있다.

현재 샘플은 단어 ID 20개·100클립이지만 중복 79개를 제거하면 21개 3D 시퀀스,
원본 동작 그룹은 20개다. 단어당 1그룹이라 독립 학습/평가에는 추가 촬영분이 필요하다.
한국어 뜻 매핑은 실제 발화 연결에 필요하며, 매핑이 없어도 단어 ID 기반 연구는 가능하다.
3D 변환 결과와 구체적인 전이 계획은 [AIHUB_TRANSFER.md](AIHUB_TRANSFER.md)를 참고한다.

- 최소 5명, 사람당 어휘별 20~30회, 날짜·배경·조명을 나누어 수집한다.
- 팀원 2명만으로 수집한 결과는 사용자 독립 성능으로 주장하지 않는다.
- 최종 평가는 같은 촬영본의 무작위 분할이 아니라 미사용 사람 또는 미사용 날짜를 통째로 뺀다.
- 청각장애인 또는 한국수어 숙련자 1명 이상에게 어휘 선정과 수형을 검수받는다.

### 4. HUD는 광학계 검증 전까지 AR이 아님 — 높음

128×64 단색 OLED 프리뷰가 읽힌다고 광학 결합기에서 읽히는 것은 아니다. 초점 거리, 밝기,
고스트 이미지, 시야각, 눈 위치 허용 범위가 모두 남아 있다. 현재 위치 보정은 2D 오프셋/스케일이라
거리와 머리 움직임에 따른 시차까지 해결하지 못한다. 발표 명칭은 실물 고정이 검증되기 전까지
“시선 근처 HUD”가 안전하다.

### 5. 실행 환경과 통합 산출물 — 높음

PyTorch·ONNX 설치 및 관통 테스트, Vosk 공식 한국어 모델, TTS 캐시 22개, Galmuri 폰트는
준비됐다. 합성음성 3문장에서도 Vosk 오인식이 있어 실제 환경의 자막 품질 개선은 남아 있다.
실제 수어 배포 모델과 카메라·마이크·OLED 동시 실행 기록도 아직 없다.
`check_env.py`의 성공 결과와 15분 연속 동작 로그를 릴리스 조건으로
둔다. `models/INNORIX-EX/`는 프로젝트 모델이 아닌 다운로드 도구 데이터이며 Git에는 무시되지만,
제출 압축본에서는 제외한다.

## MVP 성공 기준

숫자는 사전에 고정하고 결과가 낮아도 그대로 보고한다.

| 항목 | 12주 MVP 합격선 |
|---|---|
| 수어 범위 | 필수 8~10개, 경쟁 목표 15개 어휘 |
| 오프라인 분류 | 미사용 사람 또는 날짜 기준 macro F1 ≥ 0.75 |
| 무동작 안전성 | 10분 일상 동작에서 오발화 ≤ 2회 |
| 수어→소리 지연 | 단일 어휘 기준 p95 ≤ 1.5초 |
| 음성→최종 자막 지연 | 문장 종료 후 p95 ≤ 1.5초 |
| STT 품질 | 조용한 실내의 고정 시험문장 CER ≤ 25% |
| 안정성 | 카메라+STT+TTS+HUD 15분 연속, 크래시 0회 |
| 하드웨어 | 착용/휴대 가능한 고정, 노출된 점퍼선 최소화, 비상 종료 가능 |

정확도 하나만 제시하지 말고 coverage(임계값을 넘겨 실제 출력한 비율), 오발화, 지연 p50/p95,
실패 사례 영상을 함께 제시한다.

## 12주 일정

| 주차 | 팀 공통 산출물 | 통과하지 못할 때 |
|---|---|---|
| 1 | 사용자 시나리오 1개, 어휘 8~10개, 심사 메시지, 부품 주문 | 기능 추가 중단 |
| 2 | 실제 장착 시점 영상과 손/얼굴 검출률, 카메라 배치 확정 | 두 카메라/가슴 카메라/고정 자막 기본값 결정 |
| 3 | Vosk·TTS·OLED 각각 실물 동작, 한글 가독성 사진 | OLED 대신 더 큰 HUD/모니터 폴백 준비 |
| 4 | 3명 이상 1차 데이터, 첫 ONNX, 오발화 포함 기준선 | 어휘를 더 구분되는 6~8개로 축소 |
| 5 | 5명 이상 데이터, 미사용 화자/세션 평가 | 모델 확대보다 데이터·어휘 수정 |
| 6 | Phase 1 실시간 단독 데모, 지연 p50/p95 | 입력 시점·세그먼트 먼저 수정 |
| 7 | Phase 2 실시간 단독 데모, CER·자막 지연 | 고정 문장/조용한 환경으로 범위 고정 |
| 8 | 양방향 통합, 에코 게이트, 10분 소크 테스트 | 순차 모드 버튼을 폴백으로 제공 |
| 9 | 착용형 기구물, 배선·전원·발열 정리 | 연산부를 허리/노트북으로 분리 |
| 10 | 수어 숙련자/대상 사용자 평가, 실패 사례 기록 | UI·어휘를 피드백 기준으로 1회 수정 |
| 11 | 발표 영상, 포스터 그래프, 라이브/녹화 폴백, 15분 소크 | 새 기능 금지, 안정화만 수행 |
| 12 | 3회 이상 전체 리허설, 예비 케이블·전원·장치 이미지 | 제출본 동결 |

## 2인 역할 분담

- 팀원 A — 인식/데이터: 촬영 프로토콜, 라벨 검수, 학습, ONNX, 평가표와 실패 분석.
- 팀원 B — 하드웨어/런타임: 카메라 배치, OLED 광학계, 오디오, 전원, 기구물, 통합 안정성.
- 매주 마지막 반나절은 함께 실제 장치로 통합한다. 브랜치를 오래 분리한 뒤 8주차에 처음 합치지 않는다.
- 두 사람 모두 상대 영역의 실행법과 현장 복구 절차를 익힌다.

## 경진대회 발표 전략

1. 기술부터 설명하지 말고 “수어 사용자와 비수어 사용자가 병원 접수에서 겪는 단절” 한 장면으로 시작한다.
2. 양방향 왕복 대화 한 편을 60~90초 안에 보여준다.
3. 데이터 분할, 오발화, 지연, 실패 조건을 한 장의 표로 공개한다.
4. 실제 사용자 피드백이 설계를 어떻게 바꿨는지 전후를 보여준다.
5. 라이브 데모와 동일한 장치로 찍은 무편집 폴백 영상을 준비한다. `tools/demo.py` 영상은 UI 리허설용이며
   인식 성능 증거로 사용하지 않는다.

## 이번 검토에서 반영한 수정

- 보정 도구의 변경값이 실제 HUD에 반영되지 않던 객체 참조 오류 수정
- 수집 속도와 무관하게 정확히 1초를 30프레임으로 저장하도록 시간 기반 리샘플 추가
- STT 오디오 큐의 최대 지연을 8초에서 약 1초로 줄이고 TTS 중 자기 음성 유입 차단
- 위/아래 시야 이탈이 오른쪽 화살표로 표시되던 HUD 방향 오류 수정
- 잘못된 shape/라벨 순서/클래스 수로 의미 없는 학습·평가를 계속하지 않도록 실패 우선 검사 추가
- MediaPipe와 tf2onnx의 protobuf 충돌을 제거하도록 학습을 PyTorch→ONNX 단일 경로로 통일
- 환경 점검에 모델·라벨·Vosk·TTS 캐시·폰트 산출물 검사 추가
- 양수 로짓을 확률로 오인하던 추론 모호성을 제거하고 ONNX 출력 메타데이터·변환 검증 추가
- 실제 OLED에 십자 표식을 보내는 광학 캘리브레이션 경로 추가
- 카메라 없는 고정 자막 폴백과 선택형 하향 손/전방 얼굴 두 카메라 실행 경로 추가
- 어깨가 잘린 착용 시점에서도 손 특징을 유지하는 `wearable-v3` 정규화 및 버전 검사 추가
- 데이터셋 계약 파일과 촬영자/세션 식별자를 추가해 구형 전처리 혼입과 평가 누수 차단
- 실제 장착 위치의 손/얼굴 검출률·처리 FPS를 90% 게이트로 측정하는 도구 추가
