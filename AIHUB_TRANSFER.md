# AI Hub 3D 데이터 활용 검토 — 2026-09-08

이전 검토의 “3인칭이라 활용하기 어렵고, 어휘 매핑이 없어서 학습할 수 없다”는 결론을 정정한다.
보유 파일에는 실제 3D 관절 좌표가 있으며, 단어 ID로 손 모양 사전학습을 할 수 있다.
한국어 단어 뜻 매핑은 해당 ID를 음성으로 출력할 때 필요하다.

## 실제 파일에서 확인한 내용

`data/raw/New_sample`의 JSON 12,440개 전체를 검사했다.

| 항목 | 확인 결과 |
|---|---|
| 영상/키포인트 클립 | 100개 |
| 단어 ID | WORD1501~WORD1520, 20개 |
| 시점 코드 | F/D/L/R/U, 단어당 5개 |
| 사람/촬영 식별 토큰 | REAL01 하나 — 실제 인물 정의는 원본 메타데이터로 확인 필요 |
| 손 2D | 좌우 각각 21×(x,y,confidence), 63개 숫자 |
| 손 3D | 좌우 각각 21×(x,y,z,validity), 84개 숫자 |
| 추가 필드 | 몸/얼굴 3D, 카메라 Intrinsics/CameraMatrix/Distortion |
| 정확히 중복된 3D 클립 | 79개 |
| 변환 후 시퀀스 | 21개, 원본 동작 그룹은 20개 |
| 누락 프레임 | WORD1515_REAL01_L의 62~66번 |

모든 손 3D 관절의 유효 플래그는 1이었다. 이는 파일의 주석 상태이지 실제 카메라 검출률 100%를
의미하지 않는다. 카메라 시점이 달라도 같은 프레임의 3D 좌표가 동일한 경우가 대부분이다.
재구성된 공통 좌표를 여러 시점 파일에서 공유하는 것으로 해석할 수 있다.
`data/processed/aihub3d/manifest.json`에 원본 경로, 프레임 수, 해시, 그룹, 보정값을 기록했다.

## 왜 1인칭에서도 활용할 수 있나

관절의 정확한 3D 위치를 안다면 카메라 회전과 무관한 특징을 만들 수 있다.
예를 들어 관절 사이 거리를 손목~중지 MCP 거리로 나눈 값은 평행 이동·강체 회전·균일 확대에
불변이다. 새로운 `src/hand_shape3d.py`는 양손 관절 쌍 거리와 유효 마스크를 462차원으로 만든다.
회전·이동·크기 변경, 결측 관절, MediaPipe world 좌표 변환 테스트를 통과했다.

기존 `features_v2.py`는 2D x/y만 사용해 3D 정보를 버렸다. 또한 2D 평면 내 회전 정렬을
3D 시점 불변처럼 설명했다. 그 설명은 잘못됐으며 코드 주석도 정정했다.

다만 “끝점이 있으니 시점은 전혀 상관없다”가 성립하려면 다음 조건을 구분해야 한다.

- 2D 투영은 깊이를 잃는다. 2D 각도는 카메라 방향에 따라 달라진다.
- 3D 주석과 실제 단안 카메라에서 추정한 3D는 오차·가림 분포가 다르다.
- 손 모양을 회전 불변으로 만들면 손바닥 방향 정보도 사라진다. 위치·방향·궤적을 구분해야 하는
  수어에는 별도 동작 분기가 필요하다. 손 모양만으로 모든 단어를 구분할 수 있다는 보장은 없다.
- 착용자는 자신의 얼굴을 촬영하기 어렵다. 비수지 표현이 필요한 어휘는 장치의 관측 범위를 따져야 한다.

MediaPipe Hands의 `multi_hand_world_landmarks`는 이 3D 손 모양 분기에 연결할 수 있다.
현재 `main.py`의 Holistic 246차원과는 다른 계약이다. 새 모듈에는 변환 어댑터가 있지만,
새 특징으로 학습·실제 카메라 검증한 모델을 기존 런타임에 통합하는 일은 아직 남아 있다.

## 지금 실행한 작업과 다음 실험

실행:

```bash
python tools/prepare_aihub3d.py data/raw/New_sample --out data/processed/aihub3d
```

결과:

- `dataset.npz`: `X=(21,30,462)`, 단어 ID 라벨, 원본 동작 그룹
- 클립별 NPZ: 원시 3D 좌표, 원본 프레임 번호, 손 모양 특징
- `manifest.json`: 중복과 누락 프레임 감사 결과

이 결과는 **사전학습 입력 준비**다. 한국어 수어 인식 모델을 학습하거나 정확도를 검증한 결과가 아니다.
현재 표본은 단어당 원본 동작 그룹이 하나다. 다른 시점을 학습/검증으로 나누면 대부분 같은
3D 동작이 양쪽에 들어가므로 성능이 과장된다. 반복 촬영과 다른 사람의 샘플을 추가해야 한다.
전체 클립을 30개로 리샘플한 입력은 현재 런타임의 1초 창과도 다르므로 시간 구간을 맞춰야 한다.

권장 실험 순서:

1. AI Hub의 추가 화자/반복 동작과 WORD→한국어 뜻 매핑 확보.
2. 공통 3D 손 모양 인코더를 사전학습하고, 원본 동작/화자 단위로 분할.
3. 착용 시점 MediaPipe Hands 데이터로 미세조정. 손 모양 + 손목 궤적/방향 분기 결합.
4. 같은 착용 데이터 양으로 `직접 학습`과 `AI Hub 사전학습 후 미세조정`을 비교.
5. 미사용 착용자 F1, 필요한 촬영량, 오발화율을 비교해 사전학습의 실제 이득을 보고.

3D 회전 증강은 가상의 관절 좌표를 만들 수 있지만, 실제 영상의 가림과 검출 오차를 재현하지는
않는다. 실제 착용 데이터가 필요한 이유는 여기 있다. AI Hub와 직접 수집은 함께 활용할 수 있다.

## 출처

- [OpenPose 출력 형식](https://cmu-perceptual-computing-lab.github.io/openpose/web/html/doc/md_doc_02_output.html): 2D는 x/y/confidence, 3D는 x/y/z/validity.
- [MediaPipe Hands](https://chuoling.github.io/mediapipe/solutions/hands.html): 21개 3D 관절, world 좌표, 단안 추정과 가림, handedness 규약.
- 수치와 파일 구조: 본 프로젝트에 제공된 AI Hub 샘플의 직접 검사 결과.
